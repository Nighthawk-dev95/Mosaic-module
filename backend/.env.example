# These are already set as Windows user environment variables on this machine
# (see the Databricks EDW MCP server setup). This file documents what's needed
# if the module is later deployed elsewhere.
DATABRICKS_SERVER_HOSTNAME=
DATABRICKS_HTTP_PATH=
DATABRICKS_TOKEN=
MOSAIC_CATALOG=edw_dev
MOSAIC_SCHEMA=mosaictechops
